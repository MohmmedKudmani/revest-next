## Required Context Loading

Before acting on any request in this workspace, ensure the context files below have been loaded in the current session.

If these files have not already been loaded, read them in order before writing any code or running any command.

1. `context/project-overview.md` — what this frontend is, what it does, what it connects to
2. `context/architecture.md` — folder structure, data flow, component and action patterns
3. `context/code-standards.md` — Next.js conventions, server action rules, schema patterns, Shadcn usage
4. `context/ai-workflow-rules.md` — implementation order and scoping rules

After loading, follow these files as source of truth. If a request conflicts with them, ask before proceeding.

Re-read affected files when modified. Re-read all five if context may have been lost.

## Rule Ownership

All project rules live in the relevant `context/` file. Do not add rules here.
